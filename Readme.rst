.. highlight:: rst

============================
Weatherbus
============================

-----------------
Running
-----------------

#. On your system, install `podman-docker` (or just the `docker`
  if it is the tool of your choice).
#. Run the build

    .. code-block:: bash

        docker build .
